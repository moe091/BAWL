BAWL.Char = function(sprite) {
    //basic char has a single, unanimated graphic to draw.
    this.sprite = sprite;
}

//basic char just renders its graphic, which is done automatically by phaser.
BAWL.Char.prototype.draw = function() {
    //handled by phaser for basic chars. 
}